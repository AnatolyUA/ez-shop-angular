import { ConfirmableClickDirective } from './confirmable-click.directive';

describe('ConfirmableClickDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmableClickDirective();
    expect(directive).toBeTruthy();
  });
});
